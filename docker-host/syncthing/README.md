# Syncthing

Status: **live/populated**, running on docker-host.

Externally accessible at `syncthing.alarconrivero.dev` — see [`external-access/README.md`](../../external-access/README.md) for the full request path.

## TODOs

- Compose file for this stack not yet captured in this repo — *TODO (now, if not already in `/docker/syncthing/`): locate/document its actual compose.yaml and `.env` alongside the other stacks*
